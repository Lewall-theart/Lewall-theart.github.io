// src/vue-app.ts
// Vue app entry - runs on every page that has a Vue island
import type { App } from 'vue'

export default (app: App) => {
  // Register global plugins here if needed
  // e.g. app.use(router) — but we use Astro routing so not needed
}
